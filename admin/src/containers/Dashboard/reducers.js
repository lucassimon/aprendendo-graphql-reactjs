const dashboardState = {
  sellersQty: 0,
  productsQty: 0
}

const dashboard = (state = dashboardState, action) => {
  
  switch (action.type) {
    default:
      return state
  }
}


export default { dashboard }
